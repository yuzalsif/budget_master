name: budget_master
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

# -------------------- ADD THIS BLOCK --------------------
environment:
  sdk: '>=3.0.0 <4.0.0' # This is a robust way to define it
# --------------------------------------------------------

dependencies:
  flutter:
    sdk: flutter

  # Database
  isar: ^3.1.0+1
  isar_flutter_libs: ^3.1.0+1
  path_provider: ^2.0.11

  # State Management & DI
  flutter_riverpod: ^2.3.6
  
  # Other useful packages
  intl: ^0.18.1
  
dev_dependencies:
  flutter_test:
    sdk: flutter
    
  build_runner: ^2.4.6
  isar_generator: ^3.1.0+1

flutter:
  uses-material-design: true